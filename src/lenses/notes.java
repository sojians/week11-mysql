// sql injection = entering sql code when prompting for input to alter your sql statements like || 1=1 ; drop database.
// To avoid use prepared statements

// you obtain a java class connection from the DriverManager

// RDMS = relational database management
// they are useful for organizing and indexing (faster reading), avoid redundant data (normalization)